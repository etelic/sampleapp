@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_SessionClientAssociation>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=SessionClientAssociation.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_SessionClientAssociation","T_ClientID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Client"?"hidden":"")>Client</th>
}
@if(User.CanView("T_SessionClientAssociation","T_SessionID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Session"?"hidden":"")>Session</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_SessionClientAssociation","T_ClientID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Client"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_client.DisplayValue)
        </td>
}
@if(User.CanView("T_SessionClientAssociation","T_SessionID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Session"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_session.DisplayValue)
        </td>
}
                                </tr>
                            }
</table>
               
