@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_School>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=School.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_School","T_Name"))
{
		<th>Name</th>
}
@if(User.CanView("T_School","T_Code"))
{
		<th>Code</th>
}
@if(User.CanView("T_School","T_Description"))
{
		<th>Description</th>
}
@if(User.CanView("T_School","T_Address"))
{
		<th>Address</th>
}
@if(User.CanView("T_School","T_City"))
{
		<th>City</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_School","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
@if(User.CanView("T_School","T_Code"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Code)
        </td>
}
@if(User.CanView("T_School","T_Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Description)
        </td>
}
@if(User.CanView("T_School","T_Address"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Address)
        </td>
}
@if(User.CanView("T_School","T_City"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_City)
        </td>
}
                                </tr>
                            }
</table>
               
